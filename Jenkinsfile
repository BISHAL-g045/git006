pipeline {
    agent any
        stage ('Build') {
            steps {
            bat 'pwd'
            bat 'docker ps'
            bat 'dir'
            bat 'whoami'
            }
        }
    }
}
