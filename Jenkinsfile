pipeline {
    agent any
    stages{
        stage('Build') {
            steps {
            bat 'echo "Hello"'
            bat 'docker ps'
            bat 'dir'
            bat 'whoami'
            }
        }
    }
}
